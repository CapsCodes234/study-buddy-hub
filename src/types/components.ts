export interface Component {
  id: string;
  subjectId: string;
  componentName: string;
  paperCode: string;
  durationMin: number;
  totalMarks: number;
  weightingPercent: number;
  createdAt: string;
  updatedAt: string;
}
